Options -Indexes
RewriteEngine On

# Pass OPTIONS through to PHP
RewriteCond %{REQUEST_METHOD} OPTIONS
RewriteRule .* - [L]

# Route existing files normally
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Everything else to index.html
RewriteRule ^ index.html [L]
